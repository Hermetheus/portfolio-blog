#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gatsby-plugin-s3/node_modules/is-ci/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../gatsby-plugin-s3/node_modules/is-ci/bin.js" "$@"
  ret=$?
fi
exit $ret
